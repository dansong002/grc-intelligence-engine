# Optional: enables the "Deepen with AI" feature (Section 07).
# Without this key, the curated baseline stands on its own — the AI layer is additive.
# Get a key at https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=
